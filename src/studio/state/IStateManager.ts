import { ISelectedSimulationStateManager } from "./ISelectedSimulationStateManager";

export interface IStateManager {
  selectedSimulation: ISelectedSimulationStateManager;
  // Add other state slices here as needed
}
